// tests.h
#ifndef TESTS_H
#define TESTS_H

/// Call this once at startup to run all of the repository/command tests.
/// If any assertion fails, the program will abort.
void runAllTests();

#endif // TESTS_H
